# 05_edit

`trim_and_concat.sh` の出力先。各カットをトリム・速度調整・9:16化した後の `cutNN.mp4` と、最終結合済みの `forest_2dk_master_30s.mp4` が入る。

Cut 1(外観実写・3.0s)・Cut 13(CTAカード・1.5s)は Kling 生成対象外のため、9:16 に整えた状態であらかじめこのフォルダに `cut01.mp4` `cut13.mp4` として配置しておくこと。スクリプトはこの2本を存在すれば結合に含める。
